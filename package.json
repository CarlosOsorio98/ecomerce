{
  "name": "proyecto",
  "private": true,
  "scripts": {
    "start": "bun run server/app.js",
    "dev": "bun run --hot server/app.js",
    "migrate": "bun run scripts/migrate.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "sharp": "^0.34.3",
    "zod": "^4.0.5"
  }
}